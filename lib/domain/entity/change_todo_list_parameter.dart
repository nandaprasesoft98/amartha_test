class ChangeTodoListParameter {
  String todoId;
  String description;
  bool isMarked;

  ChangeTodoListParameter({
    required this.todoId,
    required this.description,
    required this.isMarked
  });
}